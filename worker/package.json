{
  "name": "vikat-agent-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Vikat pre-sales AI agent — Cloudflare Worker backend",
  "scripts": {
    "dev": "wrangler dev --env dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "test": "node --import ./test/ttf-loader.mjs --test \"test/**/*.test.js\"",
    "build:knowledge": "node ../scripts/build-knowledge.js",
    "check:abstractions": "node --import ./test/ttf-loader.mjs --test test/no-direct-bindings.test.js",
    "test:widget": "node --import ./test/ttf-loader.mjs --test test/widget.test.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.72.0",
    "@pdf-lib/fontkit": "^1.1.1",
    "fflate": "^0.8.3",
    "pdf-lib": "^1.17.1"
  },
  "devDependencies": {
    "playwright": "^1.62.1",
    "wrangler": "^4.0.0"
  }
}
